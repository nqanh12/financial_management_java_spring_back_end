# Render Blueprint: https://render.com/docs/infrastructure-as-code
# Sau khi push, vào Render Dashboard → New → Blueprint → chọn repo này → Apply.
# Bổ sung thủ công trong Environment (sync: false): GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET,
# OAUTH2_FRONTEND_REDIRECT_URL, APP_CORS_ORIGINS. Thêm redirect URI Google:
#   https://<tên-web-service>.onrender.com/login/oauth2/code/google

databases:
  - name: expense-db
    databaseName: expense
    user: expense
    plan: free
    postgresMajorVersion: "16"

services:
  - type: keyvalue
    name: expense-redis
    plan: free
    ipAllowList: []
    maxmemoryPolicy: allkeys-lru

  - type: web
    name: expense-api
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /actuator/health
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
      # Render native URL; app converts postgresql://... → JDBC + user/pass (see PostgresDatabaseUrlEnvironmentPostProcessor).
      - key: DATABASE_URL
        fromDatabase:
          name: expense-db
          property: connectionString
      - key: DATABASE_SSL_MODE
        value: require
      - key: REDIS_URL
        fromService:
          type: keyvalue
          name: expense-redis
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_CLIENT_SECRET
        sync: false
      - key: OAUTH2_FRONTEND_REDIRECT_URL
        sync: false
      - key: APP_CORS_ORIGINS
        sync: false
