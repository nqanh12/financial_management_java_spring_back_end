# Copy to application-local.yaml (gitignored) for local development.
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/expense
    username: expense
    password: expense
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: your-google-oauth-client-id
            client-secret: your-google-oauth-client-secret

app:
  jwt:
    secret: change-me-in-application-local-yaml
